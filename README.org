#+html: <p align="center"><img src="docket-logo.svg" width="200" alt="Docket logo" /></p>
#+title: Docket
#+author: Jonathan Chu

Todoist-like GTD task management for Emacs, built on org files.

Docket provides a polished, Todoist-inspired interface for managing
tasks in plain org files.  No database, no sync service — just your
org files with a modern UI on top.

#+html: <p align="center"><img src="docket-screenshot.png" width="800" alt="Docket upcoming view" /></p>

* Features

- *Projects sidebar* — ewoc-based side panel listing views, projects with task counts, and labels
- *Today view* — overdue tasks, tasks due today, and next actions in one glance
- *Upcoming view* — tasks grouped by date for the next 7 days
- *Quick add* — natural language capture: =Buy milk #errands p1 tomorrow /Shopping=
- *Filters* — filter by priority, state, tag, or project; save custom filters
- *Label browser* — see all tags across your files with counts
- *Project management* — create new projects from quick-add or =C-c d p=
- *Refile* — move tasks between projects without leaving the view
- *Sort modes* — cycle between priority, date, and project sorting
- *Completed toggle* — show/hide done tasks in any view
- *Help menus* — contextual transient popups with =?=
- *Live updates* — cache auto-refreshes when you save your org files; sidebar counts update on every action

* Requirements

- Emacs 29.1+

* Installation

Clone the repository and add it to your load path:

#+begin_src elisp
(add-to-list 'load-path "~/path/to/docket")
(require 'docket)

(setq docket-files '("~/org/gtd.org"))
(global-set-key (kbd "C-c d") docket-command-map)
(global-docket-mode 1)
#+end_src

Or with =use-package=:

#+begin_src elisp
(use-package docket
  :load-path "~/path/to/docket"
  :bind-keymap ("C-c d" . docket-command-map)
  :custom
  (docket-files '("~/org/gtd.org"))
  :config
  (global-docket-mode 1))
#+end_src

=global-docket-mode= activates =docket-mode= in any buffer visiting a file listed in =docket-files=.  Omit it if you'd rather enable =docket-mode= manually per buffer.

* Quick Start

Give docket a full try without touching any of your existing org files — the included [[https://github.com/jonathanchu/docket/blob/main/example-gtd.org][example-gtd.org]] comes fully populated with sample projects, tasks, and tags:

#+begin_src sh
cp ~/path/to/docket/example-gtd.org ~/org/gtd.org
#+end_src

#+begin_src elisp
(setq docket-files '("~/org/gtd.org"))
#+end_src

Then =C-c d d= to open the workspace and explore.

When you're ready to start for real, the barebones [[https://github.com/jonathanchu/docket/blob/main/starter-gtd.org][starter-gtd.org]] is included — copy it to your org directory and make it your own:

#+begin_src sh
cp ~/path/to/docket/starter-gtd.org ~/org/gtd.org
#+end_src

#+begin_src elisp
(setq docket-files '("~/org/gtd.org"))
#+end_src

* Org File Structure

Docket works with standard org files.  The expected convention:

#+begin_src org
,* Inbox
,** TODO Buy groceries
,** TODO Call dentist
,* Work
,** NEXT Review PR #142                                    :code:
   DEADLINE: <2026-04-05 Sat>
,** TODO Ship v2.0 release notes
,** WAIT Feedback from design team                         :design:
,* Personal
,** TODO [#A] File taxes
   DEADLINE: <2026-04-15 Tue>
,** SOMEDAY Learn piano
#+end_src

- Level 1 headings = Projects
- Sub-headings = Tasks
- TODO states: =TODO=, =NEXT=, =WAIT=, =SOMEDAY=, =DONE=, =CANCELLED=
- Priorities: =[#A]= (p1), =[#B]= (p2), =[#C]= (p3)
- Tags: standard org tags for labels
- State indicators: =●= NEXT, =○= TODO, =◑= WAIT, =◌= SOMEDAY, =◉= DONE, =⊘= CANCELLED

* Keybindings

** Global (under =C-c d= prefix)

| Key     | Command              | Description           |
|---------+----------------------+-----------------------|
| =C-c d d= | =docket-open=          | Open workspace        |
| =C-c d q= | =docket-close=         | Close workspace       |
| =C-c d a= | =docket-capture=       | Quick add task        |
| =C-c d t= | =docket-view-today=    | Today view            |
| =C-c d u= | =docket-view-upcoming= | Upcoming view         |
| =C-c d p= | =docket-create-project=  | Create new project    |
| =C-c d f= | =docket-filter=        | Apply filter          |
| =C-c d l= | =docket-view-labels=   | Browse labels         |
| =C-c d r= | =docket-refresh=       | Force refresh         |

** In task views

| Key   | Action                 |
|-------+------------------------|
| =x=     | Toggle task done/todo  |
| =RET=   | Jump to task in org    |
| =e=     | Cycle priority         |
| =t=     | Change TODO state      |
| =s=     | Set scheduled date     |
| =d=     | Set deadline           |
| =#=     | Edit tags              |
| =/=     | Move to project        |
| =S=     | Cycle sort mode        |
| =H=     | Toggle completed tasks |
| =?=     | Help menu              |
| =n= / =p= | Next / previous task   |
| =g=     | Refresh view           |
| =q=     | Close workspace        |

* Quick Add Syntax

The quick add command (=C-c d a=) parses natural language:

#+begin_example
Buy milk #errands p1 tomorrow /Shopping
#+end_example

| Token       | Meaning                            |
|-------------+------------------------------------|
| =#tag=        | Org tag                            |
| =p1= / =p2= / =p3= | Priority A / B / C                 |
| =today=       | Schedule for today                 |
| =tomorrow=    | Schedule for tomorrow              |
| =monday= etc. | Schedule for next occurrence       |
| =+3d=         | Schedule 3 days from now           |
| =!deadline=   | Next date sets deadline            |
| =/Project=    | File under project heading         |

If the project doesn't exist, docket will ask whether to create it.
You can also create an empty project with =C-c d p=.

* Configuration

#+begin_src elisp
;; Required: list of org files to use
(setq docket-files '("~/org/gtd.org" "~/org/work.org"))

;; Optional customizations
(setq docket-inbox-heading "Inbox")        ; default
(setq docket-sidebar-width 32)            ; default
(setq docket-todo-states '("TODO" "NEXT" "WAIT" "SOMEDAY"))  ; default
(setq docket-done-states '("DONE" "CANCELLED"))              ; default
#+end_src

* License

GPL v3.  See [[file:LICENSE][LICENSE]].
